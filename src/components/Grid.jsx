import React from 'react';

/**
 * Individual Grid (pixel) component of the HeatMap
 */
const Grid = () => {
    return (
        <div className={`w-[1rem] h-[1rem] bg-red-200 cursor-pointer`}>
        </div>
    );
};

export default Grid;